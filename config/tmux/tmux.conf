
# --- Truecolor (kitty) ---
set -g default-terminal "tmux-256color"
set -as terminal-features ",xterm-kitty:RGB"

# Undercurl + colored underlines (Neovim LSP diagnostics).
# Smulx/Setulc are custom caps, not covered by terminal-features -> use overrides.
set -as terminal-overrides ',*:Smulx=\E[4::%p1%dm'
set -as terminal-overrides ',*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m'

# kitty graphics protocol (image.nvim) through tmux. `on` = visible-pane only
# (`all` has a session-leak bug).
set -g allow-passthrough on

# --- QoL ---
set -g base-index 1
setw -g pane-base-index 1
set -g renumber-windows on
set -sg escape-time 0
set -g focus-events on
set -g history-limit 50000
bind r source-file ~/.tmux.conf \; display "reloaded"

# ctrl-b is a bit awkward for tmux interactions
unbind C-b

# Set tmux activation to ctrl + a
set-option -g prefix C-a
bind-key C-a send-prefix

# Splits + new window: cwd-aware (replaces default | / " binds)
unbind '"'
unbind %
bind '|' split-window -h -c "#{pane_current_path}"
bind '-' split-window -v -c "#{pane_current_path}"
bind  c  new-window     -c "#{pane_current_path}"

##### Detect Neovim #####
is_vim="ps -o state= -o comm= -t '#{pane_tty}' | \
  grep -iqE '^[^TXZ ]+ +(\\S+\\/)?g?\.?(view|n?vim?x?)(-wrapped)?(diff)?$'"

##### Navigation (Ctrl-h/j/k/l) #####
bind-key -n C-h if-shell "$is_vim" 'send-keys C-h' 'select-pane -L'
bind-key -n C-j if-shell "$is_vim" 'send-keys C-j' 'select-pane -D'
bind-key -n C-k if-shell "$is_vim" 'send-keys C-k' 'select-pane -U'
bind-key -n C-l if-shell "$is_vim" 'send-keys C-l' 'select-pane -R'

##### Resize (Alt-h/j/k/l) step = 5 #####
bind-key -n M-h if-shell "$is_vim" 'send-keys M-h' 'resize-pane -L 5'
bind-key -n M-j if-shell "$is_vim" 'send-keys M-j' 'resize-pane -D 5'
bind-key -n M-k if-shell "$is_vim" 'send-keys M-k' 'resize-pane -U 5'
bind-key -n M-l if-shell "$is_vim" 'send-keys M-l' 'resize-pane -R 5'

##### Copy-mode (vi) #####
bind-key -T copy-mode-vi C-h select-pane -L
bind-key -T copy-mode-vi C-j select-pane -D
bind-key -T copy-mode-vi C-k select-pane -U
bind-key -T copy-mode-vi C-l select-pane -R

bind-key -T copy-mode-vi M-h resize-pane -L 5
bind-key -T copy-mode-vi M-j resize-pane -D 5
bind-key -T copy-mode-vi M-k resize-pane -U 5
bind-key -T copy-mode-vi M-l resize-pane -R 5

# --- Copy mode: vi + system clipboard (+ OSC52) ---
setw -g mode-keys vi
set  -g set-clipboard on
# Portable OSC52 yank (set-clipboard on pushes to the macOS pasteboard via kitty;
# copy-pipe "pbcopy" would silently no-op over SSH).
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send -X copy-selection-and-cancel
bind -T copy-mode-vi MouseDragEnd1Pane send -X copy-selection-and-cancel


# --- Prefixless window switching ---
bind -n M-1 select-window -t 1
bind -n M-2 select-window -t 2
bind -n M-3 select-window -t 3
bind -n M-4 select-window -t 4
bind -n M-5 select-window -t 5
bind -n M-6 select-window -t 6
bind -n M-7 select-window -t 7
bind -n M-8 select-window -t 8
bind -n M-9 select-window -t 9
bind -n M-Tab last-window

# --- sesh: full multi-source picker on prefixless C-f (is_vim-guarded) ---
# fzf-tmux -p spawns the popup itself; run-shell drives sesh connect.
bind -n C-f if-shell "$is_vim" "send-keys C-f" {
  run-shell "sesh connect \"$(sesh list --icons | fzf-tmux -p 80%,70% \
    --no-sort --ansi --border-label ' sesh ' --prompt '⚡  ' \
    --header '  ^a all ^t tmux ^g configs ^x zoxide ^f find ^d kill' \
    --bind 'tab:down,btab:up' \
    --bind 'ctrl-a:change-prompt(⚡  )+reload(sesh list --icons)' \
    --bind 'ctrl-t:change-prompt(🪟  )+reload(sesh list -t --icons)' \
    --bind 'ctrl-g:change-prompt(⚙️  )+reload(sesh list -c --icons)' \
    --bind 'ctrl-x:change-prompt(📁  )+reload(sesh list -z --icons)' \
    --bind 'ctrl-f:change-prompt(🔎  )+reload(fd -H -d 2 -t d -E .Trash . ~)' \
    --bind 'ctrl-d:execute(tmux kill-session -t {2..})+change-prompt(⚡  )+reload(sesh list --icons)' \
    --preview-window 'right:55%' --preview 'sesh preview {}')\""
}
# sesh last: flip to previous session
bind L run-shell "sesh last"

# --- Popups ---
bind g     display-popup -E -d "#{pane_current_path}" -w 88% -h 80% "lazygit"
bind Enter display-popup -E -d "#{pane_current_path}" "tmux new-session -A -s scratch"

# --- Claude Code and omnigent: open current plan in nvim (new window) ---
bind P run-shell "~/.dotfiles/config/tmux/open-plan.sh '#{pane_id}' '#{pane_current_path}'"

# Enable mouse for better usage
set -g mouse on

# Don't let neovim rename windows automatically if they have a custom name associated with them.
set-option -g allow-rename off

# PLUGINS
set -g @plugin 'tmux-plugins/tpm'

# --- Persistence ---
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @resurrect-capture-pane-contents 'on'
set -g @continuum-restore 'on'
set -g @continuum-save-interval '15'

# --- Status line: AI-native, hand-rolled (native tmux formats, ~zero subprocess cost) ---
# Replaces janoamaral/tokyo-night-tmux (deleted - see ARCHITECTURE.md for the measured cost
# and the tofu-glyph bug it shipped). Palette lifted from that plugin's 'night' theme so the
# look is unchanged:
#   bg #1A1B26  fg #a9b1d6  dim #414868  sel #2A2F41
#   blue #7aa2f7  green #73daca  yellow #e0af68  red #f7768e  magenta #bb9af7  cyan #7dcfff
# Every glyph below is verified present in FantasqueSansMNerdFontMono-Regular.ttf's cmap -
# check any new glyph the same way before adding it (see CLAUDE.md footguns).
set -g status on
set -g status 2
set -g status-interval 5
set -g status-style "bg=#1A1B26,fg=#a9b1d6"

# --- line 1 (bottom): session pill (doubles as the prefix indicator) + path / git / ---
# memory / day / time / battery widgets, centered as one block. Flat Nerd Font glyphs
# throughout (not emoji) to match the minimalist macOS-menu-bar look, each widget with its
# own accent color - every widget must set its OWN #[fg=...] rather than relying on a
# neighbor's reset, or it silently inherits the previous separator's dim color. Path, day,
# and time are native formats (zero forks - %d/%H:%M go through strftime the same zero-fork
# way %s does). Git, memory, and battery are the only three subprocesses in the whole bar,
# each a small TTL-cached script (see statusbar/).

# The session pill inverts to amber while the prefix (C-a) is armed - a prefix indicator
# that costs no extra columns, since it restyles something already on the bar.
set -g status-left-length 30
set -g status-left "#[fg=#{?client_prefix,#1A1B26,#7aa2f7},bg=#{?client_prefix,#e0af68,default},bold] #S #[default]"

# --- responsive tiers -------------------------------------------------------------------
# The bar drops widgets (and tightens separators) as the client narrows, so it never
# overflows and gets clipped at the terminal edge.
#
# FOOTGUN: tmux's #{>=:x,y} / #{<:x,y} compare STRINGS, not numbers - "90" >= "100" is
# TRUE and "100" >= "80" is FALSE, both silently wrong. So every width threshold here is an
# arithmetic sign test instead: #{e|-|:width,N} goes negative iff width < N, and #{m:-*,...}
# detects that leading minus. Also note the condition of a #{?...} must be written INLINE -
# factoring it into its own option and referencing it via #{E:} does not work, because a
# bare "1"/"0" in the condition slot is looked up as a variable name, not read as a boolean.
set -g @_sb_sep "#[fg=#414868]#{?#{m:-*,#{e|-|:#{client_width},110}}, │ ,    │    }"

set -g @_sb_path "#[fg=#7dcfff] #{b:pane_current_path}"
set -g @_sb_git  "#($HOME/.dotfiles/config/tmux/statusbar/git.sh '#{pane_current_path}')"
set -g @_sb_mem  "#[fg=#73daca]#($HOME/.dotfiles/config/tmux/statusbar/memory.sh)"
set -g @_sb_date "#[fg=#7aa2f7] %d"
set -g @_sb_time "#[fg=#c0caf5] %H:%M"
set -g @_sb_batt "#($HOME/.dotfiles/config/tmux/statusbar/battery.sh)"

# >=110: everything, wide separators.  >=75: everything, tight separators.
# >=50: drop path + day.               <50: time + battery only.
set -g @_sb_full "#{E:@_sb_path}#{E:@_sb_sep}#{E:@_sb_git}#{E:@_sb_sep}#{E:@_sb_mem}#{E:@_sb_sep}#{E:@_sb_date}#{E:@_sb_sep}#{E:@_sb_time}#{E:@_sb_sep}#{E:@_sb_batt}"
set -g @_sb_mid  "#{E:@_sb_git}#{E:@_sb_sep}#{E:@_sb_mem}#{E:@_sb_sep}#{E:@_sb_time}#{E:@_sb_sep}#{E:@_sb_batt}"
set -g @_sb_min  "#{E:@_sb_time}#{E:@_sb_sep}#{E:@_sb_batt}"

set -g status-right-length 200
set -g status-right "#{?#{m:-*,#{e|-|:#{client_width},75}},#{?#{m:-*,#{e|-|:#{client_width},50}},#{E:@_sb_min},#{E:@_sb_mid}},#{E:@_sb_full}} "
set -g 'status-format[1]' "#[align=centre]#{T:status-left}#{T:status-right}"

# --- line 0 (top): the window (tab) list, full width, centered ---
# tmux 3.7's status-format[0]/[1]/[2] default to "everything in one line" / a *pane* list /
# a *session* list respectively - none of that is what we want on either line, so both
# status-format[0] (here) and status-format[1] (above) are set explicitly.
#
# State contract: a Claude Code hook (claude-tmux-state.sh) writes @claude_state /
# @claude_start / @claude_project as PANE options on UserPromptSubmit / PermissionRequest /
# Stop / SessionStart / SessionEnd, then `refresh-client -S`s. Everything below just reads
# those options back - no polling, no forks.

# @_cl_panes: space-joined @claude_state per pane in the window being rendered, but ONLY for
# panes whose foreground process is actually `claude` right now (pane_current_command is
# tracked natively by tmux - zero forks). This is the self-healing gate: if claude is killed
# or Ctrl-C'd without its Stop hook firing, @claude_state goes stale, but the moment the
# pane's foreground reverts to the shell this stops counting it, and the badge clears on the
# next redraw with no manual reset.
set -g @_cl_panes "#{P:#{?#{==:#{pane_current_command},claude},#{@claude_state}, } ,#{?#{==:#{pane_current_command},claude},#{@claude_state}, } }"

# Worst-state-wins ladder across all panes in the window: blocked > done > working > idle.
set -g @_cl_badge "#{?#{m:*blocked*,#{E:@_cl_panes}},#[fg=#f7768e] ,#{?#{m:*done*,#{E:@_cl_panes}},#[fg=#73daca] ,#{?#{m:*working*,#{E:@_cl_panes}},#[fg=#7aa2f7]●,#[fg=#414868]·}}}#[fg=#a9b1d6]"

# project:command, e.g. "ambidiff:claude" - falls back to #W untouched if the window was
# manually renamed (prefix ,), since that turns automatic-rename off for that window. Project
# and command are colored separately so the label reads as two distinct pieces of info, not
# one flat blob.
set -g @_cl_label "#{?automatic-rename,#[fg=#7dcfff]#{?@claude_project,#{@claude_project},#{b:pane_current_path}}#[fg=#414868]:#[fg=#787c99]#{pane_current_command}#[fg=#a9b1d6],#[fg=#a9b1d6]#W}"

# Elapsed since @claude_start, ticking in status-interval (5s) steps at zero extra cost.
# NOTE: @claude_start is pane-scoped. In a split where the focused pane isn't the Claude
# pane, the badge still reflects it via the @_cl_panes loop above, but the timer (which reads
# the window's *active* pane via fallthrough) won't render for it. Acceptable trade-off.
set -g @_cl_timer "#{e|/|:#{e|-|:%s,#{@claude_start}},60}m#{e|m|:#{e|-|:%s,#{@claude_start}},60}s"

set -g window-status-format "#{E:@_cl_badge} #I #{E:@_cl_label}#{?@claude_start, #[fg=#c0caf5]#{E:@_cl_timer}#[fg=#a9b1d6],}"
set -g window-status-current-format "#[bg=#3b4261,bold] #{E:@_cl_badge} #I #{E:@_cl_label}#{?@claude_start, #[fg=#c0caf5]#{E:@_cl_timer}#[fg=#a9b1d6],} #[bg=#1A1B26,nobold]"
set -g window-status-separator "    "

# `range=window|...` markers carried over from tmux's own default status-format[0] verbatim -
# drop them and prefix+click / mouse tab-select breaks (mouse is on, see :113).
set -g 'status-format[0]' "#[align=centre]#[list=on align=centre]#[list=left-marker]<#[list=right-marker]>#[list=on]#{W:#[range=window|#{window_index}]#{T:window-status-format}#[norange]#{?loop_last_flag,,#{E:window-status-separator}},#[range=window|#{window_index}]#{T:window-status-current-format}#[norange]#{?loop_last_flag,,#{E:window-status-separator}}}"

# --- Hint-based copy/open (Vimium-style, prefix+F) ---
set -g @plugin 'Morantron/tmux-fingers'

# Import TPM
run '~/.tmux/plugins/tpm/tpm'
