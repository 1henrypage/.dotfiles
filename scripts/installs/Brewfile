# Options
cask_args appdir: '~/Applications', require_sha: true

#############################################################
# Command Line                                              #
#############################################################

# CLI Essentials
brew 'gh'           # GitHub CLI
brew 'git'          # Version controll
brew 'tmux'         # Term multiplexer
brew 'wget'         # wget

# CLI Basics
brew 'fzf'          # Fuzzy file finder and filtering
brew 'jq'           # JSON parser, output and query files
brew 'ripgrep'      # Searching within files (better grep)
brew 'fd'           # Fast find (better find), used by sesh picker
brew 'tree'         # Directory listings as tree structure
brew 'trash-cli'    # Record and restore removed files
brew 'unzip'        # Unzip
brew 'watch'        # Run commands periorically
brew 'zoxide'       # Auto-learning navigation (better cd)
brew 'starship'     # Prompt
brew 'beads'        # Agentic issue tracker
brew 'sesh'         # Smart tmux session manager (zoxide-backed)
brew 'lazygit'      # Terminal UI for git (tmux popup)
brew 'FelixKratz/formulae/borders' # Focused-window border daemon (AeroSpace companion)

# CASKS
cask 'visual-studio-code' # Code editor
cask 'kitty'              # GPU Accelerated Terminal Emulator
cask 'sublime-text'       # Text editor, will migrate off this soon
cask 'nikitabobko/tap/aerospace' # Tiling window manager (not in cask core)

# Development Langs, Compilers, Package Managers and SDKs
brew 'jenv'
brew 'openjdk@17'
brew 'openjdk@21'
brew 'rustup'
brew 'uv'
brew 'node'
brew 'go'
# TODO fix other language configurations later

# Media
brew 'ffmpeg'        # Video/audio converter

# Browsers
cask "firefox@developer-edition"

# Fonts
cask 'font-fantasque-sans-mono-nerd-font'
